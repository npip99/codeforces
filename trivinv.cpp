#include <iostream>

using namespace std;

int arr[200005];

int main() {
  int t;
  cin >> t;
  for( int tc = 1; tc <= t; tc++ )
  {
    int n;
    cin >> n;
    for( int i = 0; i < n; ++i ) {
      cin >> arr[i];
    }
    int ret = 0;
    for( int i = 0; i < n; ++i ) {
      
    }
  }
}